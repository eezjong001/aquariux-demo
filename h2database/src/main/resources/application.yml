spring:
  application:
    name: h2database-service
  main:
    allow-bean-definition-overriding: 'true'
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: password
  h2:
    console:
      enabled: true
server:
  port: '8080'